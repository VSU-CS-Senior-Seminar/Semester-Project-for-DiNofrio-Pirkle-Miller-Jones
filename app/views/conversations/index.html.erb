<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href=<%= newsfeed_index_path %>>Good <%=(03..15).include?(Time.now.hour) ? 'Morning' : 'Afternoon'%>, <%= current_user.name %></a>
      
      <%= link_to edit_user_registration_path(current_user) do %>
        <img class="settings-logo" src="/assets/settings.png">
      <% end %>
     
      
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'NewsFeed', newsfeed_index_path %></li>
        <li><%= link_to 'Posts', posts_path %></li>
      </ul>
       
      </form>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Inbox", conversations_path %></li>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div><h1>Your Conversations</h1></div>
  <div class="container2">
    <p><%= link_to 'Start conversation', new_message_path, class: 'btn btn-lg btn-primary' %></p>
    <ul class="list-group">
      <%= render partial: 'conversations/conversation', collection: @conversations %>
  </ul>
</div>
<%= will_paginate %>